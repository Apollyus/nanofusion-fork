{
  "name": "nano-flow-boost-editable",
  "version": "1.0.0",
  "description": "Editable local clone of NANO Conversion Hub",
  "scripts": {
    "dev": "vite --host --port 5173",
    "build": "node sync-content.js && shx mkdir -p dist && shx cp -r public/assets public/static dist/ && shx cp -r index.html o-nas.html faq sluzby dist/ && cd admin-panel && npm install --legacy-peer-deps && npm run build",
    "preview": "vite preview",
    "format": "prettier --write \"index.html\" \"assets/*.js\" \"assets/*.css\" \"manifest.json\""
  },
  "keywords": [
    "nano",
    "frontend",
    "vite"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "prettier": "^3.8.1",
    "shx": "^0.4.0",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3"
  }
}
